# Optional Maven Central mirror used while building the Docker image.
# Leave empty to use Maven's default repository configuration.
# Example: default behavior (no mirror)
# MAVEN_MIRROR_URL=
# Example: Google Maven Central mirror
# MAVEN_MIRROR_URL=https://maven-central.storage-download.googleapis.com/maven2/
MAVEN_MIRROR_URL=
